
"use strict"




var states = require('jst/commons/constants').grammar.selectorStates




var states = {

	[states.TYPE]: {
		states.TYPE,
		states.OPERATOR,
		states.PSEUDOCLASS
	},

	[states.PSEUDOCLASS]: {
		states.STRING,
		states.ATTRIBUTE_KEY,
		states.OPERATOR,
		states.TYPE
	},

	[states.OPERATOR]: {
		states.TYPE
	},

	[states.ATTRIBUTE_KEY]: {
		states.STRING
	},

	[states.STRING]: {
		states.TYPE,
		states.OPERATOR
	}

}





module.exports = states
